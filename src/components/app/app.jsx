import React from 'react';

import { Header } from '../header';

import './app.css';

export const App = (props) => (
    <div className="app">
        <Header />
        компонент App
        </div>
);